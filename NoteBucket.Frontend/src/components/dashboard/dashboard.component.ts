import {Component} from '@angular/core';

@Component({
    selector: 'dashboard',
    template: `<div>If this would be a real app, you would see some fancy stuff here.</div>`
})
export class DashboardComponent {
}